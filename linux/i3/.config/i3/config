# General settings
set $mod Mod1
set $term st
font pango:JetBrains Mono Nerd Font 10

# Gaps and borders
# gaps inner 10
# gaps outer 5
gaps top 28
# gaps top 30
# new_window none
# new_float none
# hide_edge_borders both

# General window settings
# for_window [class=".*"] border pixel 0 
for_window [all] border pixel 1

# Window management
bindsym $mod+shift+c kill
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# bindsym $mod+Shift+h move left
# bindsym $mod+Shift+j move down
# bindsym $mod+Shift+k move up
# bindsym $mod+Shift+l move right
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split
bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+space floating toggle

# Workspace management
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10


# Dunst toggle
# bindsym $mod+Shift+d exec dunstctl set-paused toggle


# bindsym $mod+Shift+c reload
bindsym $mod+Shift+i restart

# Screenshot keybindings
bindsym Print exec flameshot gui

# Launch programs
bindsym $mod+Shift+Return exec $term
bindsym $mod+p exec rofi -show combi -combi-modi

# System controls
# bindsym $mod+Shift+s exec i3lock --nofork -tiling --image='/home/mufeedcm/.config/backgrounds/kid_in_town.jpg' & systemctl suspend
bindsym $mod+Shift+s exec --no-startup-id ~/.config/scripts/confirm.sh 'Sleep' "mpc pause && slock & systemctl suspend"
bindsym $mod+Shift+l exec --no-startup-id ~/.config/scripts/confirm.sh 'Exit i3' 'i3-msg exit'
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+u exec --no-startup-id ~/.config/scripts/confirm.sh 'Shutdown' 'systemctl poweroff'
bindsym $mod+Shift+r exec --no-startup-id ~/.config/scripts/confirm.sh 'Reboot' 'systemctl reboot'

bindsym $mod+w exec --no-startup-id ~/.config/scripts/wifi_menu.sh
bindsym $mod+o exec --no-startup-id ~/.config/scripts/ncmpcpp_toggle.sh
bindsym $mod+Shift+b exec --no-startup-id ~/.config/scripts/books_list.sh
bindsym $mod+Shift+v exec --no-startup-id ~/.config/scripts/clipmenu.sh sel
bindsym $mod+n exec --no-startup-id ~/.config/scripts/emacs_toggle.sh
# Display controls
bindsym $mod+Shift+h exec --no-startup-id ~/.config/scripts/display_control/display_control.sh
bindsym $mod+comma exec --no-startup-id ~/.config/scripts/display_control/display_adjust.sh down
bindsym $mod+period exec --no-startup-id ~/.config/scripts/display_control/display_adjust.sh up

# Volume controls
bindsym $mod+Shift+k exec --no-startup-id ~/.config/scripts/volume_bar.sh up
bindsym $mod+Shift+j exec --no-startup-id ~/.config/scripts/volume_bar.sh down
bindsym $mod+Shift+m exec --no-startup-id ~/.config/scripts/volume_bar.sh toggle
bindsym $mod+s exec --no-startup-id ~/.config/scripts/audio-switcher.sh toggle
# set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status


mode "resize" {
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym j resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Start essential applications
exec_always feh --bg-scale ~/.config/backgrounds/kid_in_town.jpg 
exec_always --no-startup-id killall polybar; ~/.config/polybar/launch_polybar.sh --reload
exec --no-startup-id nm-applet
exec --no-startup-id dunst
exec --no-startup-id numlockx
exec --no-startup-id flameshot
# exec_always --no-startup-id picom --config ~/.config/picom/picom.conf --daemon
# exec_always --no-startup-id picom --config ~/.config/picom/picom.conf
exec --no-startup-id ~/.config/scripts/display_control/restore_state.sh
# exec_always --no-startup-id autotiling
exec --no-startup-id dex --autostart --environment i3
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork --tiling --image='/home/mufeedcm/.config/backgrounds/mistbg_blurred.png'
exec --no-startup-id xautolock -time 15 -locker "slock & systemctl suspend"
exec --no-startup-id amixer set Headphone 30% unmute 
exec --no-startup-id st -c StWinTmux -e zsh -ic tmux
exec --no-startup-id zen-browser
# exec --no-startup-id emacs --daemon
exec --no-startup-id opentabletdriver-daemon
exec --no-startup-id ~/.config/scripts/clipwatch.sh &
exec_always --no-startup-id bash -c 'pgrep -x buckle > /dev/null || buckle -g 40 &'
exec --no-startup-id QT_QPA_PLATFORMTHEME=qt5ct

assign [class="zen"] $ws2
for_window [instance="StWinTmux"] move container to workspace number $ws1
for_window [class="^ncmpcpp$"] floating enable, move position 1418 40, resize set 500 500


set $bg_dark       #1a1b26
set $bg            #16161e
set $bg_highlight  #3c3836
set $terminal_black #282828
set $fg            #ebdbb2
set $fg_dark       #a89984
set $fg_gutter     #7c6f64
set $red           #fb4934
set $orange        #fe8019
set $yellow        #fabd2f
set $green         #b8bb26
set $blue          #83a598
set $purple        #d3869b
set $cyan          #8ec07c
set $error         #9d0006

# Window colors
client.focused          $bg_dark    $fg        $bg_dark       $fg        $fg
client.focused_inactive $bg_highlight $fg_dark $bg_highlight $fg_gutter $fg_gutter
client.urgent           $red        $fg        $red           $error     $fg
client.placeholder      $bg_highlight $fg_dark $fg_gutter    $fg_gutter $fg_gutter
client.background       $bg_dark
